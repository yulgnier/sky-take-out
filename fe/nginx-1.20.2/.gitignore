# 前端项目通用忽略项
# 依赖包（通过 npm/yarn 安装，无需上传）
node_modules/
.pnp/
.pnp.js

# 构建产物（通常通过构建命令生成，无需上传）
dist/
build/
out/
dist-ssr/
*.local

# 环境变量文件（可能包含敏感信息如 API 密钥等）
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# 日志和缓存文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.eslintcache
*.log

# 编辑器/IDE 配置文件（本地个性化配置，无需共享）
.idea/
.vscode/
*.swp
*.swo
*.sublime-*
.DS_Store  # Mac 系统文件
Thumbs.db  # Windows 系统文件


# Nginx 相关忽略项
# Nginx 运行时生成的文件（日志、临时文件等）
nginx/logs/
nginx/temp/
nginx/run/
nginx/*.pid  # 进程 ID 文件

# Nginx 本地调试配置（非通用配置，如本地路径等）
nginx/conf.d/*.local.conf
nginx/conf/*.local
nginx/custom.local.conf

# Nginx 证书文件（如果有，通常包含敏感信息，建议单独管理）
nginx/ssl/  # 若证书需要共享，可删除此行并单独列出不需要忽略的文件